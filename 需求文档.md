# 目标

1. 设计并实现一个可以在 Web 上执行本地终端的应用。
2. 采用本地 Agent 结合 WebServer 的方式实现，本地 Agent 负责与WebServer通信，并且执行开启终端和执行终端命令。WebServer 负责管理 Agent，提供交互式界面。Web 界面可以采用 xterm.js 来模拟终端界面效果。


# 核心思路

1. Agent 和 WebServer 采用 TCP 自定义协议方式进行通信。
2. WebServer和 Agent 的通信采用 Jnet 框架，WebServer 的 web 服务相关内容采用 JfireBoot 框架。
